# Only needed for a deployed build. In dev, vite.config.ts proxies /api and /ws
# to http://localhost:8000, so leave this unset and the app uses relative URLs.
#
# Set it to the backend origin (no trailing slash). The WebSocket URL is derived
# from it by swapping http -> ws.
VITE_API_URL=https://api.consensus.example
